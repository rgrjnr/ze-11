<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% if title %}
                {{ title }} -
            {% endif %}
            {{ site.title or "11ty Site" }}</title>
        <meta
        name="description" content="{{ description or site.description or 'A website built with 11ty' }}"> <!-- Tailwind CSS -->
        <link rel="stylesheet" href="https://use.typekit.net/hpo8xtc.css">
        <link
        rel="stylesheet" href="/assets/css/style.css">
        <!-- Custom TypeScript -->
        <script src="/dist/main.js" type="module"></script>
        <script nomodule src="/dist/main-legacy.js"></script>
    </head>
    <body class="{{ tags }}">
        <div id="smooth-wrapper">
            <div id="smooth-content">
                <header class="container">
                    <nav class="flex justify-between items-center">
                        <a href="/" class="text-xl font-bold flex items-center gap-2">
                            {% include "src/assets/images/logo-home.svg" %}
                            {{ site.title or "11ty Site" }}
                        </a>
                        <div class="flex items-center space-x-4">
                            <a href="/" class="text-gray-600 hover:text-blue-600">Home</a>
                            <a href="/cases/" class="text-gray-600 hover:text-blue-600">Cases</a>
                        </div>
                    </nav>
                </header>
                <main class="container">
                    <h1 class="text-4xl font-bold text-center my-24" data-line-split>{{ title }}</h1>
                    <img src="{{ image }}" alt="{{ title }}" class="w-full h-auto">
                    <div class="content">
                        {{ content | safe }}
                    </div>
                </main>
                <footer class="container">
                    <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
                        <p class="text-center text-sm">&copy; {{ site.year or '2024' }}
                            {{ site.title or "11ty Site" }}. All rights reserved.</p>
                    </div>
                </footer>
            </div>
        </div>
    </body>
</html>