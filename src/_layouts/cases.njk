<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% if title %}
                {{ title }} -
            {% endif %}
            {{ site.title or "11ty Site" }}</title>
        <meta name="description" content="{% if excerpt %}{{ excerpt }}{% else %}{{ description or site.description or 'UX/UI Designer José Ferreira crafts accessible digital experiences—from museum sites to fintech—offering UX research, prototyping & design systems expertise.' }}{% endif %}">
        <meta
        name="author" content="José Ferreira"> <!-- Open Graph / Facebook -->
        <meta property="og:type" content="website">
        <meta property="og:url" content="https://joseferreira.io{{ page.url }}">
        <meta property="og:title" content="{% if title %}{{ title }} - José Ferreira{% else %}José Ferreira - UX/UI Designer{% endif %}">
        <meta property="og:description" content="{% if excerpt %}{{ excerpt }}{% else %}{{ description or site.description or 'UX/UI Designer José Ferreira crafts accessible digital experiences—from museum sites to fintech—offering UX research, prototyping & design systems expertise.' }}{% endif %}">
        <meta
            property="og:image"
            content="https://joseferreira.io{% if image %}{{ image }}{% else %}/assets/images/og.jpg{% endif %}">
        <meta property="og:image:width" content="1200">
        <meta property="og:image:height" content="630">
        <meta property="og:image:type" content="image/jpeg">
        <meta property="og:image:alt" content="{% if title %}{{ title }} - José Ferreira{% else %}José Ferreira - UX/UI Designer{% endif %}">
        <meta property="og:site_name" content="José Ferreira">
        <meta
        property="og:locale" content="en_US"> <!-- Twitter -->
        <meta property="twitter:card" content="summary_large_image">
        <meta property="twitter:url" content="https://joseferreira.io{{ page.url }}">
        <meta property="twitter:title" content="{% if title %}{{ title }} - José Ferreira{% else %}José Ferreira - UX/UI Designer{% endif %}">
        <meta property="twitter:description" content="{% if excerpt %}{{ excerpt }}{% else %}{{ description or site.description or 'UX/UI Designer José Ferreira crafts accessible digital experiences—from museum sites to fintech—offering UX research, prototyping & design systems expertise.' }}{% endif %}">
        <meta
            property="twitter:image"
            content="https://joseferreira.io{% if image %}{{ image }}{% else %}/assets/images/og.jpg{% endif %}">
        <meta property="twitter:creator" content="@joseferreira">
        <meta
        property="twitter:site" content="@joseferreira"> <!-- Additional Meta Tags -->
        <meta name="theme-color" content="#3B82F6">
        <meta name="msapplication-TileColor" content="#3B82F6">
        <link
        rel="canonical" href="https://joseferreira.io{{ page.url }}"> <!-- Tailwind CSS -->
        <link rel="stylesheet" href="https://use.typekit.net/hpo8xtc.css">
        <link
        rel="stylesheet" href="/assets/css/style.css">
        <!-- Custom TypeScript -->
        <script src="/dist/main.js" type="module"></script>
        <script nomodule src="/dist/main-legacy.js"></script>
    </head>
    <body class="{{ tags }}">
        <div
            id="smooth-wrapper">
            <!-- Progress Bar -->
            <div class="progress-bar-container fixed top-0 left-0 w-full z-50">
                <div class="progress-bar h-1 bg-blue-600 origin-left"></div>
            </div>
            <div id="smooth-content">
                <header class="container">
                    <nav class="flex justify-between items-center">
                        <a href="/" class="text-xl font-bold flex items-center gap-2">
                            {% include "src/assets/images/logo-home.svg" %}
                            {{ site.title or "11ty Site" }}
                        </a>
                        <div class="flex items-center space-x-4">
                            <a href="/" class="text-gray-600 hover:text-blue-600">Home</a>
                            <a href="/cases/" class="text-gray-600 hover:text-blue-600">Cases</a>
                        </div>
                    </nav>
                </header>
                <main class="container">
                    <h1 class="text-4xl font-bold text-center my-24" data-line-split style="text-wrap: balance;">{{ title }}</h1>
                    <img src="{{ image }}" alt="{{ title }}" class="w-full h-auto  rounded-lg">
                    <div class="content">
                        {{ content | safe }}
                    </div>
                    {# Case Navigation #}
                    {% from "components/case-navigation.njk" import caseNavigation %}
                    {{ caseNavigation(collections, page.url) }}
                </main>
                <footer class="container">
                    <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
                        <p class="text-center text-sm">&copy; {{ site.year or '2024' }}
                            {{ site.title or "11ty Site" }}. All rights reserved.</p>
                    </div>
                </footer>
            </div>
        </div>
    </body>
</html>